from setuptools import setup, find_packages

setup(
    name="AEye",
    version="0.1",from setuptools import setup, find_packages

setup(
    name="AEye",
    version="0.1",
    packages=find_packages(),
)

    packages=find_packages(),
)
